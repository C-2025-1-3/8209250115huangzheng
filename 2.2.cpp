#include<iostream>
using namespace std;
int main()
{
	float x, y;
	cin >> x;
	if (x > 0 && x < 10)
	{
		if (x < 1, y = 3 - 2 * x);
		else if (x >= 1 && x < 5, y = 1 + 2 / (4 * x));
		else (x<10&&x>=5,y = x * x);
		cout << y << endl;
	}
	     else cout << "²»ÔÚ·¶Î§ÄÚ" << endl;
	return 0;
}